{
  "name": "client-sdk-web-vapi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/"
  },
  "dependencies": {
    "@vapi-ai/web": "^2.1.6"
  }
}
